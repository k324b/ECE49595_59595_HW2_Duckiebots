// generated from rosidl_adapter/resource/msg.idl.em
// with input from duckietown_msgs/msg/SignalsDetection.msg
// generated code does not contain a copyright notice

#include "std_msgs/msg/Header.idl"

module duckietown_msgs {
  module msg {
    module SignalsDetection_Constants {
      @verbatim (language="comment", text=
        "For the first backoff approach" "\n"        "string led_detected" "\n"        "string no_led_detected" "\n"        "Each of these can be:")
      const string NO_CAR = "no_car_detected";
      const string SIGNAL_A = "car_signal_A";
      const string SIGNAL_B = "car_signal_B";
      const string SIGNAL_C = "car_signal_C";
      const string SIGNAL_PRIORITY = "car_signal_priority";
      const string SIGNAL_SACRIFICE_FOR_PRIORITY = "car_signal_sacrifice_for_priority";
      const string NO_CARS = "no_cars_detected";
      const string CARS = "cars_detected";
      const string NO_TRAFFIC_LIGHT = "no_traffic_light";
      const string STOP = "tl_stop";
      const string GO = "tl_go";
      const string YIELD = "tl_yield";
    };
    struct SignalsDetection {
      std_msgs::msg::Header header;

      @verbatim (language="comment", text=
        "this is what we can see at the intersection:")
      string front;

      string right;

      string left;

      @verbatim (language="comment", text=
        "Plus we can see the traffic light" "\n"
        "for the moment we assume that no traffic light exists")
      string traffic_light_state;
    };
  };
};
